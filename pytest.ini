[pytest]
# Run from the repo root:  pytest -q
# The suite is pure-Python (no TestRail/Jira calls, no Streamlit runtime), so it
# runs in seconds and is safe to execute before every push.
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
